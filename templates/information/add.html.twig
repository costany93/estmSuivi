{% extends "base.html.twig" %}

{% block body %}
    <div class="container body-begin">
        {% include "shared/_navPillClub.html.twig" with {'slug': club.slug} %}
        <h1>Nouvelle information</h1>
        <div class="row">
            <div class="col-md-6">
                {{form_start(form)}}
                    {{form_widget(form)}}
                    <div class="row justify-content-center">
                        <button type="submit" class="btn btn-primary">Ajouter une nouvelle</button>
                    </div>
                {{form_end(form)}}
            </div>
        </div>
    </div>
{% endblock %}